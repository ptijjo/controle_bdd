/** Nom du cookie httpOnly contenant le refresh token (navigateur). */
export const AUTH_REFRESH_COOKIE = 'refresh_token';

/** Path restreint pour limiter l’envoi du cookie aux routes d’auth. */
export const AUTH_REFRESH_COOKIE_PATH = '/auth';
